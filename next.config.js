/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
  reactStrictMode: true,
  images: {
    // You can allow external domains here if you plan to source images
    domains: [],
  },
};

module.exports = nextConfig;